# TripTogether 客户端环境变量模板
# 用法：复制为 .env 并填入真实值（cp .env.example .env）
# 注意：.env 含密钥，应被 .gitignore 忽略，不要提交！

# 腾讯地图 JS API GL Key（浏览器侧，公开可暴露，需在腾讯位置服务控制台配置「授权域名白名单」）
# 若不配置，前端将回退到内置默认 Key，本地 / 未配置环境仍可直跑。
# 这是「前端地图渲染与路线规划」用的 Key，与服务端 WebService Key（服务端 TENCENT_MAP_KEY 环境变量）不是同一类产品。
VITE_TENCENT_MAP_KEY=

# API 基址（生产构建时设置；不设则默认 /api，本地 dev 走 vite 代理）
# 共享服务器部署约定：前端走 /trip/ 前缀，后端 API 走 /trip-api/ 前缀（nginx 剥前缀后转发到本机 :4100）。
# 因此生产构建：VITE_API_BASE_URL=/trip-api/api
VITE_API_BASE_URL=
