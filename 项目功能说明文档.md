# TripTogether 项目功能说明文档

> 本文档从《产品分析与实施计划》中提取全部功能项，并与当前代码实现逐一对照，标注完成状态。  
> 用于自测验收和后续功能开发追踪。
>
> **最后更新**: 2026-08-03  
> **当前版本**: MVP 开发中

---

## 状态标记说明

| 标记      | 含义                            |
| ------- | ----------------------------- |
| ✅ 已完成   | 功能已实现，前后端均已可用                 |
| 🔶 部分完成 | 后端 API 或数据模型已就绪，但前端 UI 缺失或不完整 |
| ❌ 未完成   | 尚未开始开发                        |
| 🔜 计划中  | 属于后续版本（V1.1 / V2.0 / V3.0）的功能 |

---

## 一、用户与计划管理

| #   | 功能点                 | 优先级 | 状态      | 说明                                                                    |
| --- | ------------------- | --- | ------- | --------------------------------------------------------------------- |
| 1.1 | 用户注册/登录（昵称+密码）       | MVP | ✅ 已完成   | 昵称 + 密码注册登录（SHA-256），localStorage 持久化会话。微信扫码 / 邮箱 / JWT 为后续规划 |
| 1.2 | 创建旅游计划（名称/日期/目的地）   | MVP | ✅ 已完成   | 支持填写名称、日期范围、目的地、描述。创建后自动生成每日日程骨架和 8 位邀请码，创建者自动成为 owner                |
| 1.3 | 邀请成员加入（邀请码）         | MVP | ✅ 已完成   | 生成 8 位邀请码，支持通过 `/join` 页面输入邀请码加入计划。加入者自动获得 editor 角色                  |
| 1.4 | 邀请成员加入（分享链接/免注册查看）  | MVP | ✅ 已完成   | 详情页"分享"按钮生成 `/share/:inviteCode` 只读链接，任何人免登录即可查看计划                              |
| 1.5 | 成员权限管理（创建者/编辑者/查看者） | V2  | ❌ 未完成   | 数据模型中有 role 字段（owner/editor/viewer），加入时默认 editor，但无 UI 管理入口，无法修改成员角色  |
| 1.6 | 删除旅行计划              | MVP | ✅ 已完成   | 后端 `DELETE /trips/:tripId` + 首页卡片悬浮删除按钮（带二次确认）                            |
| 1.7 | 编辑计划基本信息            | MVP | ✅ 已完成   | 后端 `PATCH /trips/:tripId` + 详情页"编辑旅行计划"Modal（标题/描述/日期/目的地/状态）            |

---

## 二、进度看板

| #   | 功能点       | 优先级 | 状态    | 说明                                            |
| --- | --------- | --- | ----- | --------------------------------------------- |
| 2.1 | 规划环节分类    | MVP | ✅ 已完成 | 4 个环节卡片：出发交通 🚀 / 住宿安排 🏨 / 每日日程 📅 / 返程交通 🏠 |
| 2.2 | 每个环节的状态标记 | MVP | ✅ 已完成 | 状态判定：无数据=待规划(灰) / 有数据=已预订(蓝) / 全部完成=已确认(绿)    |
| 2.3 | 整体进度百分比展示 | MVP | ✅ 已完成 | 进度看板顶部新增「整体规划完成度」百分比（以交通+住宿+所有日程项为粒度统计已完成占比），蓝色进度条可视化；与 7.2 仪表盘共同构成完成率视图 |
| 2.4 | 环节负责人指派   | V2  | ❌ 未完成 | 无 assignee 字段，无指派 UI                          |
| 2.5 | 环节截止时间提醒  | V2  | ❌ 未完成 | 无 due_date 字段，无提醒功能                           |
| 2.6 | 进度变更通知    | V2  | ❌ 未完成 | 无通知系统                                         |

---

## 三、交通管理

| #   | 功能点                | 优先级    | 状态      | 说明                                                                                             |
| --- | ------------------ | ------ | ------- | ---------------------------------------------------------------------------------------------- |
| 3.1 | 出发交通（方式/班次/时间/费用）  | MVP    | ✅ 已完成   | 支持 6 种交通方式（飞机/火车/大巴/自驾/轮船/其他），可记录出发地、到达地、时间、费用、状态、预订信息、备注                                      |
| 3.2 | 回程交通（方式/班次/时间/费用）  | MVP    | ✅ 已完成   | 同出发交通，在"交通安排"Tab 中分为"出发交通"和"返程交通"两个区域                                                          |
| 3.3 | 城际交通（城市间移动方式）      | MVP    | ✅ 已完成 | 前端交通 Tab 已新增「城际交通」区域（`renderSection("intercity",...)`），可录入城市间移动（飞机/火车等），与出发/返程并列；类型与状态字段通用 |
| 3.4 | 景点间交通（每日景点间如何到达）   | MVP    | ✅ 已完成   | `ScheduleItem` 已含 `transportToNext` 字段；日程项表单新增「到下一站交通」输入框（如「地铁2号线/步行10分钟」），卡片与只读分享页均展示 |
| 3.5 | 交通状态标记             | MVP    | ✅ 已完成   | 4 级状态：待规划 → 已预订 → 已确认 → 已完成                                                                    |
| 3.6 | 交通票据信息记录           | V2     | ✅ 已完成   | bookingInfo 字段已实现，表单中有"预订信息"输入框（航班号/车次号/订单号）                                                   |
| 3.7 | 交通预订链接接入（12306/携程） | Future | ❌ 未完成   | 未接入任何外部预订平台                                                                                    |

---

## 四、住宿管理

| #   | 功能点                  | 优先级    | 状态    | 说明                                 |
| --- | -------------------- | ------ | ----- | ---------------------------------- |
| 4.1 | 住宿信息录入（酒店/民宿/地址）     | MVP    | ✅ 已完成 | 可记录住宿名称、地址、入住日期、退房日期、费用、状态、预订信息、备注 |
| 4.2 | 住宿状态标记               | MVP    | ✅ 已完成 | 3 级状态：待规划 → 已预订 → 已确认              |
| 4.3 | 住宿费用记录               | MVP    | ✅ 已完成 | cost 字段已实现，在表单和卡片中展示               |
| 4.4 | 房间分配（谁住哪间）           | V2     | ❌ 未完成 | 无 room_assignment 字段，无分配 UI        |
| 4.5 | 住宿预订链接接入（Booking/美团） | Future | ❌ 未完成 | 未接入任何外部预订平台                        |

---

## 五、每日日程

| #   | 功能点             | 优先级 | 状态      | 说明                                                                                                                  |
| --- | --------------- | --- | ------- | ------------------------------------------------------------------------------------------------------------------- |
| 5.1 | 按天展示日程时间轴       | MVP | ✅ 已完成   | 按天排列卡片，每张卡片显示"第N天"+ 日期 + 星期，日程项按 sortOrder 排序                                                                       |
| 5.2 | 添加日程项（景点/餐饮/活动） | MVP | ✅ 已完成   | 弹出模态框填写：类型、名称、时间、地点、花费、状态、备注                                                                                        |
| 5.3 | 日程项类型分类         | MVP | ✅ 已完成   | 5 种类型：🍜 美食 / 🏛️ 景点 / 🎯 活动 / 🚗 交通 / 😴 休息                                                                        |
| 5.4 | 日程项状态标记         | MVP | ✅ 已完成   | 3 级状态：待规划 → 已确认 → 已完成。可一键切换"已完成/待规划"                                                                                |
| 5.5 | 日程项拖拽排序         | MVP | ✅ 已完成   | 后端 `POST /schedules/:id/items/reorder` + 前端 @dnd-kit 拖拽排序已接入日程列表                      |
| 5.6 | 日程项费用记录         | MVP | ✅ 已完成   | cost 字段已实现，在表单、卡片和行程总览中展示                                                                                           |
| 5.7 | 日程项备注与图片        | V2  | 🔶 部分完成 | 备注（description）已实现，但无图片上传功能（ScheduleItem 无 imageUrl 字段）                                                             |
| 5.8 | 日程项评论/讨论        | V2  | ❌ 未完成   | 无评论实体和 API                                                                                                          |

---

## 六、地图与路线

> **地图是 TripTogether 最具差异化的核心视图**——它把分散在「日程 / 交通 / 住宿」里的所有地点一次性、按天、按颜色直观地呈现出来，是用户「一眼看懂整个计划」的关键入口。
>
> **现状**：地图已全面落地——底层组件 `client/src/components/MapView.tsx` 基于 **腾讯地图 JS API GL（GCJ-02）**，支持按天彩色标注 / 连线、点击弹窗、跨天轨迹串联、空状态与加载失败提示；坐标录入（搜索定位 + 地图点选）与「🗺️ 地图」独立 Tab（筛选 / 图例 / 连线开关）均已可用，并已在「行程总览」与「只读分享页」（`ShareTrip`）中渲染。详见下方实现说明。

| #   | 功能点                  | 优先级 | 状态      | 说明                                                                                              |
| --- | -------------------- | --- | ------- | ----------------------------------------------------------------------------------------------- |
| 6.1 | 所有地点地图标注             | MVP | ✅ 已完成   | `MapView` 渲染带坐标的地点；住宿与日程项均支持「搜索定位」（腾讯地点搜索）回填 lat/lng，无需手填 |
| 6.2 | 按天不同颜色连线             | MVP | ✅ 已完成   | 已接入腾讯地图 JS API GL，按 `dayIndex` 用 8 色调色板生成标记与当日折线，自动 `fitBounds` 适配视野 |
| 6.3 | 点击标记查看详情             | MVP | ✅ 已完成   | 标记点击弹出 InfoWindow 显示标题 + 所属天数；住宿 / 景点以颜色区分                        |
| 6.4 | 整体路线总览图              | MVP | ✅ 已完成   | 已嵌入「🗺️ 地图」独立 Tab（新增）、「行程总览」与「只读分享页」，自动收集住宿 + 日程坐标渲染       |
| 6.5 | 距离 / 时间预估            | V2  | ❌ 未完成   | 需接入腾讯 / 高德路线规划 API，计算相邻点驾车 / 步行距离与耗时                              |
| 6.6 | 独立地图 Tab（筛选 + 图例）     | MVP | ✅ 已完成   | 顶部「🗺️ 地图」Tab：按天筛选、显隐连线、住宿 / 景点颜色图例，复用 `MapView`              |
| 6.7 | 日程项坐标录入 + 地点搜索       | MVP | ✅ 已完成   | `LocationPicker` 调用腾讯 `TMap.service.Search` 关键词搜索，回填 GCJ-02 坐标；住宿复用同一组件 |
| 6.8 | 跨天 / 住宿→次日连线        | MVP | ✅ 已完成   | 按 `checkInDate` 把住宿锚定到入住当天作为末站，相邻行程天「最后一点→次日首点」用灰色虚线串联，呈现完整移动轨迹；城际交通仅存地名无坐标，暂未串联 |
| 6.9 | 地图点选落点              | MVP | ✅ 已完成   | `MapPicker` 组件：在地图弹窗点击落标记，确认回填 GCJ-02 经纬度（best-effort 逆地理编码补地址）；日程项 / 住宿表单均接入 |
| 6.10 | 城际交通地理编码 + 跨城连线    | V2  | ❌ 未完成   | `Transportation` 增经纬度字段 + 地名地理编码自动回填；地图新增「城际层」蓝色粗线连接城市间位移，与当日彩线 / 跨天灰虚线分层；详细规格见 6.10 小节 |

### 地图功能实现说明（已落地：腾讯地图 JS API GL）

**技术选型已定**：由原先的 Leaflet + OpenStreetMap（WGS-84）**切换为腾讯地图 JS API GL（GCJ-02）**，全网坐标系统一，无需任何坐标转换。Key 已写入 `client/src/utils/tencentMap.ts`，地点搜索复用同一 Key。

**⚠️ 你需在「腾讯位置服务」控制台开通以下项（否则地图 / 搜索会 401 或额度不足）：**

1. **应用管理 → 我的应用 → 编辑该 Key** → 勾选启用产品：
   - **JavaScript API**（地图显示必需）
   - **WebServiceAPI**（地点搜索 / 地理编码底层依赖）
2. **授权域名（Referer 白名单）**：在 Key 设置里填前端域名。
   - 开发阶段：`http://localhost:5175/*` 和 `http://127.0.0.1:5175/*`
   - 上线后：改为正式域名（如 `https://your-domain.com/*`）
3. **配额管理 → 账户额度 → 一键分配**：把 **JavaScript API** 与 **WebService（地点搜索 / 地理编码）** 的免费额度分配给该 Key（个人开发者有免费额度，但需手动分配才生效）。
4. **Key 安全提醒**：这是前端 JS Key，会暴露到浏览器，务必配好 referer 白名单防盗用产生费用；生产阶段可再加**签名校验（sk）**。

**坐标系统一（已解决）**：腾讯底图与腾讯地点搜索返回坐标同为 GCJ-02，全程统一，不再出现 OSM 时代的 WGS-84 偏移问题。

**已实现代码清单：**
- `client/src/utils/tencentMap.ts`：Key 常量 + `loadTencentMap()` 动态加载脚本（`libraries=service`）+ `hexToRgbNum` 颜色转换。
- `client/src/components/MapView.tsx`：腾讯 `MultiMarker`/`MultiPolyline`/`InfoWindow` + `fitBounds`，按天彩色、空状态、加载失败提示。
- `client/src/components/LocationPicker.tsx`：腾讯 `TMap.service.Search` 关键词搜索，选中回填 GCJ-02 坐标。
- `client/src/components/MapPicker.tsx`：腾讯地图点击选点，确认回填 GCJ-02 经纬度（逆地理编码补地址）。
- `client/src/pages/TripDetail.tsx`：日程项 / 住宿表单接入「搜索定位」+「地图选点」；新增「🗺️ 地图」独立 Tab（按天筛选 + 连线开关 + 颜色图例 + 跨天灰线图例）；`MapTab` 按 `checkInDate` 把住宿锚定到对应天末站以串联跨天轨迹。

**后续（未做）：**
- 6.5 距离 / 时间预估：需路线规划 API（腾讯 / 高德），建议列入 **V2**。
- 城际交通串联：当前 `Transportation` 仅存 `departurePlace`/`arrivalPlace` 地名、**无经纬度**，需先做地理编码（地名→坐标）才能把「城际交通两端」画成跨城连线，列入 **V2**。
- 依赖清理：`leaflet` / `@types/leaflet` 已不再使用，可保留无害或后续移除。

### 6.10 城际交通地理编码 + 跨城连线（V2 详细功能规格）

> 现状缺口：6.8 已完成「住宿→次日景点」的跨天灰色虚线串联，但**城际交通两端仍是断点**——`Transportation` 表只存 `departurePlace`/`arrivalPlace` 两个**地名字符串，没有经纬度**，地图无法把它画成连接两座城市的线。6.10 就是补上这一段，让地图呈现「从出发城市 → 目的地城市 → 各城市内部每日轨迹」的完整移动全貌。

#### 一、功能点拆解

1. **`Transportation` 增加经纬度字段**
   - 新增 `depLat/depLng`（出发地坐标）、`arrLat/arrLng`（到达地坐标），类型 `float nullable`。
   - 与 `ScheduleItem`/`Accommodation` 的 `lat/lng` 坐标体系一致（均为腾讯 GCJ-02）。

2. **地名自动地理编码（核心）**
   - 交通表单的「出发地 / 到达地」输入框接入腾讯 `Tenant.service.Search`（与 `LocationPicker` 同套），用户选地名后**自动回填对应经纬度**，无需手填坐标。
   - 兼容：若用户直接手填地名字符串而未选 POI，则提交时后端/前端触发一次地理编码兜底（地名→坐标），保证连线有坐标可画。

3. **跨城连线渲染**
   - `MapView` 新增「城际层」：把每段城际交通的 `(depLat,depLng) → (arrLat,arrLng)` 画成**跨城连线**。
   - 三种线分层清晰：① 当日彩色实线（城市内每日轨迹）② 跨天灰色虚线（住宿→次日首点）③ 城际蓝色粗线（城市间位移），三者颜色/线型区分。

4. **城际标记与信息窗**
   - 城际段的起讫点用「🚉 车站/机场」专属图标标记；点击 InfoWindow 显示：交通方式图标、出发地→到达地、班次时间、费用、状态。
   - 「🗺️ 地图」Tab 图例新增「城际移动（蓝线）」一项。

5. **与 6.5 距离/时间预估联动（可选增强）**
   - 跨城连线旁可标注驾车/高铁预估耗时与距离（来自腾讯路线规划 API），让"跨城要花多久"一目了然。

#### 二、为什么必须做（必要性）

- **地图闭环的最后一块**：没有它，地图只能呈现"每个城市内部怎么玩"，城市与城市之间是空白断点，用户无法从地图上感知"整趟旅程的空间跨度"，与"一眼看懂全程"的核心卖点相悖。
- **复用已验证的链路**：地理编码（`LocationPicker`）和跨天连线逻辑（6.8）都已跑通，本功能是在其之上做"城际层"扩展，成本可控、风险低。
- **协作场景刚需**：多人跨城出游（如北京→成都玩 5 天→回北京）是最高频场景，城际段可视化是这类行程的骨架。

#### 三、依赖与前置

| 依赖项 | 说明 | 状态 |
| --- | --- | --- |
| 腾讯位置服务 WebService（地理编码） | 地名→坐标 | Key 已申请，控制台需开通 WebServiceAPI + 分配额度 |
| WebServiceAPI 配额 | 免费额度需手动分配 | 同 Key 统一开通 |
| `Transportation` 经纬度字段 | 数据模型改造 | 需加字段（synchronize 自动建列）|
| 6.5 路线规划 API（可选） | 跨城耗时/距离标注 | 需额外开通"路线规划"服务，建议放本功能二期 |

#### 四、实现方案（落地路径）

- **后端**：`Transportation` 实体加 `depLat/depLng/arrLat/arrLng`；`POST/PATCH /transportations` 接收并存储坐标（地理编码兜底在服务端或前端做均可，建议前端选 POI 时回填，后端做容错）。
- **前端表单**：交通 Tab 的「出发地/到达地」改用 `LocationPicker`（复用现有组件），选地名即带出坐标；保留"仅填地名"降级路径。
- **地图组件**：`MapView` 新增 `intercitySegments` 入参（或复用 `transportations` 传入），用 `MultiPolyline` 画蓝色跨城线 + 城际标记；与现有当日线、跨天线共用图层开关。
- **图例/筛选**：`MapTab` 图例补充城际蓝线；筛选逻辑支持按"是否显示城际段"开关。

#### 五、边界与取舍

- **仅存地名无坐标时**：不画该段连线（避免把"北京"画到海上），地图给出"该段暂无坐标"的轻提示，不影响其他层。
- **国际/偏远地名**：腾讯地理编码对国内覆盖好，境外可能失败，失败时降级为仅显示地名文本。
- **与 6.8 关系**：6.8 完成"城市内轨迹串联"，6.10 完成"城市间轨迹串联"，二者合并即"完整移动轨迹"，建议同一迭代内连续交付。

---

## 七、行程总览与导出

| #   | 功能点              | 优先级 | 状态      | 说明                                                    |
| --- | ---------------- | --- | ------- | ----------------------------------------------------- |
| 7.1 | 行程计划单（结构化展示全部计划） | MVP | ✅ 已完成   | "行程总览"Tab 展示：标题区、统计概览（天数/安排数/住宿数/总花费）、交通概要、每日行程明细、邀请码 |
| 7.2 | 进度总览仪表盘          | MVP | ✅ 已完成 | 进度看板已具备「整体规划完成度」百分比（2.3 落地），4 环节状态卡片 + 顶部进度条构成仪表盘式可视化；分享页同步展示 |
| 7.3 | 导出 PDF/图片        | V2  | ❌ 未完成   | 无导出功能                                                 |
| 7.4 | 分享链接（只读视图）       | MVP | ✅ 已完成   | 详情页「分享」按钮生成 `/share/:inviteCode` 只读链接，任何人免登录即可查看（同 1.4，已落地 `ShareTrip` 页面） |

---

## 八、协作与通知

| #   | 功能点            | 优先级 | 状态    | 说明                                     |
| --- | -------------- | --- | ----- | -------------------------------------- |
| 8.1 | 实时协作编辑（多人同时编辑） | V2  | ❌ 未完成 | 无 WebSocket / Socket.IO 集成，多人编辑需手动刷新页面 |
| 8.2 | 编辑冲突提示         | MVP | ✅ 已完成 | `ScheduleItem` 新增 `version` 乐观锁字段；后端 PATCH 检测版本冲突返回 409（带 latest 数据），前端编辑弹窗捕获后展示冲突提示，支持「用我的版本覆盖（force）」或「放弃修改并刷新」 |
| 8.3 | 操作历史记录         | V2  | ❌ 未完成 | 无 ActivityLog 实体（计划文档中有设计但未实现）         |
| 8.4 | 变更通知（站内/邮件/微信） | V2  | ❌ 未完成 | 无通知系统                                  |

---

## 九、费用管理

| #   | 功能点        | 优先级 | 状态      | 说明                                                                                                   |
| --- | ---------- | --- | ------- | ---------------------------------------------------------------------------------------------------- |
| 9.1 | 费用记录与分类    | MVP | ✅ 已完成   | 后端：Expense 实体 + CRUD API + 6 种分类（餐饮/交通/住宿/门票/购物/其他）。前端：Trip 详情「花费」Tab 已接入，支持增删改与选成员分摊              |
| 9.2 | 总预算与实际花费对比 | MVP | ✅ 已完成   | `Trip.budgetTotal` 字段（**非必填**，避免强制校验）；行程总览展示「预算使用」进度条（已花费/预算）并补充「剩余」金额，超支变红提示；全局搜索与编辑均无强制预算校验 |
| 9.3 | AA 分账计算    | V2  | ✅ 已完成   | 后端：`GET /trips/:id/expenses/stats` 按人统计（付款/应付/净额）；前端：「花费」Tab 顶部统计卡 + 点击人均/总花费弹出按人明细 Modal |

---

## 十、其他功能

| #    | 功能点       | 优先级  | 状态    | 说明                                     |
| ---- | --------- | ---- | ----- | -------------------------------------- |
| 10.1 | 操作指南页面    | —    | ✅ 已完成 | `/guide` 路由，8 个可折叠章节 + 分步操作弹窗，导航栏有入口按钮 |
| 10.2 | 移动端响应式适配  | V1.1 | ✅ 已完成 | 全站响应式：移动端单列卡片 + 折叠导航 + 全屏 Modal，手机端可用                       |
| 10.3 | 暗色模式      | V2   | ❌ 未完成 | 未实现                                    |
| 10.4 | AI 智能行程规划 | V3.0 | ❌ 未完成 | 未实现                                    |
| 10.5 | 行程模板市场    | V3.0 | ❌ 未完成 | 未实现                                    |
| 10.6 | 小程序版本     | V3.0 | ❌ 未完成 | 未实现                                    |
| 10.7 | 多语言支持     | V3.0 | ❌ 未完成 | 未实现                                    |

---

## 汇总统计

| 状态      | 数量     | 占比   |
| ------- | ------ | ---- |
| ✅ 已完成   | 41     | 67%  |
| 🔶 部分完成 | 1      | 2%   |
| ❌ 未完成   | 19     | 31%  |
| 🔜 计划中  | 0      | 0%   |
| **合计**  | **61** | 100% |

---

## MVP 功能完成度

按 MVP 优先级功能统计（排除 V2 / Future 项）：

| 状态         | 数量     |
| ---------- | ------ |
| ✅ 已完成      | 37     |
| 🔶 部分完成    | 0      |
| ❌ 未完成      | 0      |
| **MVP 合计** | **37** |

### MVP 未完成清单

> ✅ **MVP 阶段功能已全部完成**（37/37）。以下为已收尾的 5 项关键功能回顾：
> - **2.3 整体进度百分比**：进度看板顶部新增整体完成度进度条。
> - **3.3 城际交通 UI**：交通 Tab 新增「城际交通」录入区。
> - **3.4 景点间交通**：日程项「到下一站交通」录入，记录每日景点间移动方式。
> - **8.2 编辑冲突提示**：`ScheduleItem.version` 乐观锁 + 后端 409 冲突检测 + 前端冲突弹窗。
> - **9.2 总预算与实际对比**：`budgetTotal`（非必填）+ 预算使用进度条 + 剩余金额。
>
> 后续迭代（V1.1 / V2 / V3）见各章节未完成项，其中「城际交通地理编码 + 跨城连线」详细规格见第六章 6.10。

---

## 技术架构现状

| 层级     | 计划方案                       | 实际方案                  | 说明               |
| ------ | -------------------------- | --------------------- | ---------------- |
| 前端框架   | React 18 + TypeScript      | React 18 + TypeScript | ✅ 一致             |
| UI 组件库 | Ant Design / Shadcn UI     | Ant Design 5          | ✅ 一致             |
| 状态管理   | Zustand + React Query      | Zustand               | ⚠️ 缺 React Query |
| 地图     | 腾讯地图 / 高德 JS SDK（生产）       | 腾讯地图 JS API GL（已接入 `MapView` + `LocationPicker`，GCJ-02 坐标统一） | ✅ 已切换腾讯地图 |
| 拖拽交互   | dnd-kit                    | 已接入（日程拖拽排序）          | ✅ 一致             |
| 后端框架   | NestJS                     | Express + TypeScript  | ⚠️ 轻量化，可迁移       |
| ORM    | TypeORM                    | TypeORM               | ✅ 一致             |
| 数据库    | PostgreSQL                 | PostgreSQL            | ✅ 一致             |
| 缓存     | Redis                      | 未使用                   | ❌ MVP 可不需要       |
| 实时通信   | Socket.IO (MVP) / Yjs (V2) | 未接入                   | ❌ 待开发            |
| 认证     | NextAuth / JWT             | 昵称 + 密码（SHA-256 + localStorage） | 🔶 轻量方案，待增强       |
| 部署     | Vercel + 云函数               | 本地开发                  | ❌ 待部署            |

---

## 数据模型实现对照

| 计划实体           | 实现状态  | 说明                                          |
| -------------- | ----- | ------------------------------------------- |
| Trip           | ✅ 已实现 | 缺 budget_total / currency / share_link 字段   |
| TripMember     | ✅ 已实现 | 缺 invited_by 字段                             |
| User           | ✅ 已实现 | 仅 nickname + avatar，无认证字段                   |
| DailySchedule  | ✅ 已实现 | 缺 notes / weather_cache 字段                  |
| ScheduleItem   | ✅ 已实现 | 含 lat/lng（坐标，已通过 `LocationPicker`/`MapPicker` 录入）；**不含** `transport_to_next`（3.4 景点间交通待做）；缺 `image_url`（5.7 待做） |
| Transportation | ✅ 已实现 | 完整                                          |
| Accommodation  | ✅ 已实现 | 含 lat/lng（已通过搜索定位 / 地图选点录入）；缺 room_assignment（4.4 待做） |
| Expense        | ✅ 已实现 | 完整（payerId / participantIds / category）     |
| PlanningPhase  | ❌ 未实现 | 进度看板是前端计算的，无独立实体                            |
| ActivityLog    | ❌ 未实现 | 无操作日志                                       |

---

## 自测检查清单

以下为建议的自测流程，按操作顺序排列：

### 基础流程

- [ ] 首页加载正常，显示空状态
- [ ] 点击"创建计划"，填写表单，创建成功后跳转详情页
- [ ] 详情页顶部显示标题、状态、日期、目的地、邀请码、成员头像
- [ ] 邀请码点击可复制到剪贴板
- [ ] 进度看板 4 个卡片显示"待规划"状态

### 日程管理

- [ ] 每日日程 Tab 显示对应天数的卡片
- [ ] 点击"添加安排"，填写表单，保存成功
- [ ] 日程项显示类型图标、名称、状态标签、时间、地点、花费
- [ ] 点击 ✓ 可标记完成/取消完成
- [ ] 点击 ✏️ 可编辑日程项
- [ ] 点击 🗑️ 可删除日程项

### 交通管理

- [ ] 交通安排 Tab 显示"出发交通"和"返程交通"两个区域
- [ ] 添加出发交通，填写方式/出发地/到达地/时间/费用，保存成功
- [ ] 交通卡片显示方式图标、路线、时间、费用、状态
- [ ] 编辑和删除交通记录正常

### 住宿管理

- [ ] 住宿安排 Tab 可添加住宿
- [ ] 填写名称/地址/入住退房日期/费用，保存成功
- [ ] 住宿卡片显示名称、日期、地址、费用、状态
- [ ] 编辑和删除住宿记录正常

### 行程总览

- [ ] 行程总览 Tab 显示统计数据（天数/安排数/住宿数/总花费）
- [ ] 交通概要列出所有交通记录
- [ ] 每日行程列出所有日程项
- [ ] 底部显示邀请码

### 邀请加入

- [ ] 复制邀请码，打开新浏览器窗口
- [ ] 在首页点击"输入邀请码"，输入邀请码
- [ ] 加入成功后跳转到计划详情页
- [ ] 成员头像组显示新成员

### 操作指南

- [ ] 导航栏显示"操作指南"按钮
- [ ] 点击进入指南页面，显示 8 个可折叠章节
- [ ] 点击"查看详细步骤"按钮，弹出分步操作弹窗
